"""Namespace package marker for local testing.

This file ensures `custom_components` is recognized as a package when running
tests or tools that import modules as `custom_components.<domain>`. It is not
deployed to Home Assistant via install.sh.
"""

